# Default environment variables for dev/runtime wiring.
# Non-secret defaults are checked in. Override machine-local values in .env.dev.

NAMESPACE=poesis
DEPLOY_ENV=preprod
RELEASE=itip-web-frontend

CHART=./ops/helm
PORT_FORWARD_PID_FILE=.dev-port-forwards.pids

FRONTEND_PORT=4200
